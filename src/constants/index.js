'use strict';

const NAME = 'vuepress';

module.exports = {
    NAME,
    FILENAMES: [
        'micro-app.config.js',
        'micro-app.extra.config.js',
        `micro-app.${NAME}.config.js`,
    ],
};
